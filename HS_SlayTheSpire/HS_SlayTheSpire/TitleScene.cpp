#include "stdafx.h"
#include "TitleScene.h"

void TitleScene::UpdateScene(const float deltaTime)
{
}

bool TitleScene::Init()
{
	return false;
}

bool TitleScene::OnChangeScene(SceneManager * manager)
{
	if (manager == nullptr)
		return false;

	return false;
}
